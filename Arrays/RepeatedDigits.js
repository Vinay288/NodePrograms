let array = new Array();
let lowerLimit = 10;
let upperLimit = 100;
for (let index = lowerLimit; index <= upperLimit; index++)
    if (index % 10 == parseIntindex / 10)
        array.push(index)
console.log(array)